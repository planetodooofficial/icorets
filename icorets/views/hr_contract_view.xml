<?xml version="1.0" encoding="UTF-8" ?>
<odoo>

    <record id="view_hr_contract_form" model="ir.ui.view">
        <field name="name">hr.contract.form</field>
        <field name="model">hr.contract</field>
        <field name="inherit_id" ref="hr_contract.hr_contract_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//group[@name='salary']" position="inside">
                <field name="total_wage_in_days" string='Total Wage in Days'/>
                <field name="total_leave" string='Total Leave'/>
                <field name="present_days" string='Present Days'/>
                <field name="expense" string='Expense'/>
                <field name="total_earned_ctc" string='Total Earned CTC'/>
            </xpath>
        </field>
    </record>

</odoo>